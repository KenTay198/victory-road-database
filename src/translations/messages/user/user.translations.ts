interface IUserTranslations {
  forms: {
    register: {
      toasts: {
        success: string;
        error: string;
        loading: string;
      };
    };
  };
}

export default IUserTranslations;
